# วิธีการส่งงาน (GitHub & Deployment)

คุณสามารถนำงานนี้ขึ้น GitHub และทำลิงก์ให้คุณครูดูได้โดยทำตามขั้นตอนนี้นะครับ

## 1. นำโค้ดขึ้น GitHub
ผมเตรียมโค้ดทั้งหมดให้พร้อมแล้ว คุณเพียงต้องรันคำสั่งเหล่านี้ใน Terminal ของโปรเจกต์ครับ:

```bash
git add .
git commit -m "Update project: feature checkout, cart, and dark mode"
git push origin main
```

## 2. การทำให้เว็บไซต์ "ออนไลน์" (Deployment)
แนะนำให้ใช้ **Vercel** ครับ เพราะฟรีและใช้งานง่ายมากสำหรับ Next.js:

1. เข้าเว็บไซต์ [vercel.com](https://vercel.com) และสมัครใช้งานด้วย GitHub
2. กดปุ่ม **"Add New"** -> **"Project"**
3. เลือก Repository ที่ชื่อ `next-project-it`
4. กด **"Deploy"**

### ⚠️ ข้อควรระวัง (สำคัญมาก!)
เนื่องจากโปรเจกต์นี้ใช้ **SQLite** (ฐานข้อมูลแบบไฟล์):
- **บน Vercel**: ข้อมูลจะถูกดึงไปแสดงผลได้ปกติ (Login ได้, ดูสินค้าได้) แต่ถ้ามีการเพิ่มออเดอร์ใหม่ หรือสมัครสมาชิกใหม่ ข้อมูลนั้นจะ **ไม่บันทึกถาวร** (จะหายไปเมื่อระบบรีสตาร์ท)
- **วิธีแก้ที่ดีที่สุด**: ถ้าต้องการให้บันทึกข้อมูลได้ถาวร ควรเปลี่ยนไปใช้ฐานข้อมูล Cloud เช่น **Supabase** หรือ **Neon** (ถ้าคุณต้องการ ผมสามารถช่วยไกด์วิธีตั้งค่าให้ได้ครับ)

---
**คำแนะนำ:** สำหรับการส่งงานอาจารย์ หากเน้นที่การโชว์ UI และการทำงานเบื้องต้น วิธีนี้ถือว่าเพียงพอแล้วครับ!
